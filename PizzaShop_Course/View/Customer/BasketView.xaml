<UserControl x:Class="PizzaShop_Course.View.Customer.BasketView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PizzaShop_Course.View.Customer"
             mc:Ignorable="d"
             d:DesignHeight="359" d:DesignWidth="800">
    <Grid>
        <ListBox Background="#606060" Margin="0,0,0,87" ItemsSource="{Binding Items}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="300"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Label Content="{Binding Name}"/>
                        <Image Source="{Binding Image}"/>
                        <Label Content="{Binding Price}"/>
                        <Label Content="{Binding Ingredients}"/>
                        <!--add split and set auto-->
                        <Label Content="Size: "/>
                        <ComboBox ItemsSource="{Binding Size}"/>
                        <Button />
                    </Grid>

                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button Content="Order" Width="100" HorizontalAlignment="Center" Margin="350,287,350,22" />
    </Grid>
</UserControl>
