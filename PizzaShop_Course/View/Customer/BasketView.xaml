<UserControl x:Class="PizzaShop_Course.View.Customer.BasketView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PizzaShop_Course.View.Customer"
             xmlns:vm="clr-namespace:PizzaShop_Course.ViewModel"
             mc:Ignorable="d" Height="359" MinWidth="200">
    <UserControl.DataContext>
        <vm:BasketViewModel/>
    </UserControl.DataContext>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20 0">
            <ItemsControl ItemsSource="{Binding OrderItems}" Height="309">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="10" Width="130" Height="220">
                            <StackPanel>
                                <Border Width="100" Height="100" CornerRadius="5" Margin="5">
                                    <Border.Effect>
                                        <DropShadowEffect ShadowDepth="1"/>
                                    </Border.Effect>
                                    <Border.Background>
                                        <ImageBrush ImageSource="{Binding ItemPhoto}"/>
                                    </Border.Background>
                                </Border>
                                <Label Content="{Binding ItemName}"/>
                                <Label Content="{Binding Price}"/>
                                <Label Content="{Binding ItemSize}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
    <!--<Grid>
        <ListBox Background="#606060" Margin="0,0,0,87" ItemsSource="{Binding OrderItems}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Label Content="{Binding ItemName}"/>
                        <Image Source="{Binding ItemPhoto}"/>
                        <Label Content="{Binding Price}"/>
                        <Label Content="{Binding ItemSize}"/>
                        <Button />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>-->
</UserControl>
