<UserControl x:Class="PizzaShop_Course.View.UserInformationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PizzaShop_Course.View"
             xmlns:vm="clr-namespace:PizzaShop_Course.ViewModel.Administrator"
             xmlns:model="clr-namespace:PizzaShop_Course.Model"
             mc:Ignorable="d" 
             d:DesignHeight="359" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:UserViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Image Source="{Binding Photo}" Height="90" Width="90" Margin="149,44,561,225"/>
        <StackPanel Margin="141,138,0,0" VerticalAlignment="Top" HorizontalAlignment="Left">
            <Label Content="First Name"/>
            <TextBlock Text="{Binding FirstName}"/>
            <Label Content="Second Name"/>
            <TextBlock Text="{Binding LastName}"/>
            <Label Content="Email"/>
            <TextBlock Text="{Binding Email}"/>
            <Label Content="Number"/>
            <TextBlock Text="{Binding Number, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>
    </Grid>
</UserControl>
