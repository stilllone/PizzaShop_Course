<UserControl x:Class="PizzaShop_Course.View.UserInformationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PizzaShop_Course.View"
             xmlns:vm="clr-namespace:PizzaShop_Course.ViewModel.Administrator"
             xmlns:model="clr-namespace:PizzaShop_Course.Model"
             mc:Ignorable="d" 
             d:DesignHeight="359" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:UserViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Image Source="{Binding CurrentUser.Photo}" Height="90" Width="90" Margin="175,26,535,243"/>
        <StackPanel Margin="167,124,0,0" VerticalAlignment="Top" HorizontalAlignment="Left">
            <!--total spend, address some info-->
            <Label Content="First Name"/>
            <TextBlock Text="{Binding CurrentUser.FirstName}"/>
            <Label Content="Second Name"/>
            <TextBlock Text="{Binding CurrentUser.LastName}"/>
            <Label Content="Email"/>
            <TextBlock Text="{Binding CurrentUser.Email}"/>
            <Label Content="City"/>
            <TextBlock Text="{Binding CurrentUser.Id}"/>
        </StackPanel>
    </Grid>
</UserControl>
