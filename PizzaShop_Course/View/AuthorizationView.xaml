<UserControl x:Class="PizzaShop_Course.View.AuthorizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PizzaShop_Course.View"
             xmlns:adminvm="clr-namespace:PizzaShop_Course.ViewModel.Administrator"
             mc:Ignorable="d" 
             d:DesignHeight="359" d:DesignWidth="800">
    <UserControl.DataContext>
        <adminvm:UserViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0" Grid.Column="0" Content="Username:" Margin="182,109,549,-135"/>
        <TextBox Grid.Row="0" Grid.Column="0"  Text="{Binding Login}" Width="200" Margin="244,113,294,-131"/>
        <Label Grid.Row="0" Grid.Column="0" Content="Password:" Margin="182,148,549,-174"/>
        <TextBox Grid.Row="0" Grid.Column="0" Text="{Binding Password}" Width="200" Margin="244,148,294,-174"/>
        <Button Grid.Row="0" Grid.Column="0" Command="{Binding AuthorizeCommand}" Content="Login" Margin="325,210,375,-230"/>
    </Grid>
</UserControl>
